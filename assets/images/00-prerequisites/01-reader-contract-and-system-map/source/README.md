# Image prompts

Each prompt file is written before generation. It specifies the repository banner's approachable technical-cartoon style, exact labels, layout, constraints, and unwanted elements for its corresponding generated PNG.
