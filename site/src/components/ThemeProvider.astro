---
import { Icon } from '@astrojs/starlight/components';
---

{/* Inlined to prevent theme flicker on load */}
<script is:inline>
	window.StarlightThemeProvider = (() => {
		const storedTheme =
			typeof localStorage !== 'undefined' && localStorage.getItem('starlight-theme');
		// Default to light theme for canonical illustrated handbook experience
		const theme = storedTheme || 'light';
		document.documentElement.dataset.theme = theme === 'dark' ? 'dark' : 'light';
		return {
			updatePickers(t = storedTheme || 'light') {
				document.querySelectorAll('starlight-theme-select').forEach((picker) => {
					const isDark = t === 'dark';
					const btn = picker.querySelector('.theme-toggle-switch');
					if (btn) {
						btn.setAttribute('aria-checked', isDark ? 'true' : 'false');
					}
					const select = picker.querySelector('select');
					if (select) select.value = t;
				});
			},
		};
	})();
</script>

<template id="theme-icons">
	<Icon name="sun" class="light" />
	<Icon name="moon" class="dark" />
	<Icon name="laptop" class="auto" />
</template>
